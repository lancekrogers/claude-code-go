.env
.claude/
ai_docs/
it_works/

# Build artifacts
bin/
build/
coverage/
*.out
coverage.html

# Binaries (prevent top-level binary creation from manual go build)
claude-go
*.exe

# Prevent any binary with common patterns in top-level
*-example
*-demo
*_test
*_test.exe

# Demo recording intermediate files (only GIFs needed)
*.cast
